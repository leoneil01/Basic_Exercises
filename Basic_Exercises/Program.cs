using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.IO;
using System.Linq;
using System.Runtime.Remoting.Metadata.W3cXsd2001;
using System.Text;
using System.Threading.Tasks;

namespace Basic_Exercises
{
    class Program
    {
    	public static void Main(string[] args) {
		    // Program sortNum = new Program();
		    
		    // int[] x = sortNum.SortNumbers(new int[] {-5, 236, 120, 70, -5, -5, 698, 280});
		    // Console.WriteLine("Array: [{0}]", string.Join(", ", x));
		    // Console.WriteLine("Sorted Array: ");
		    // foreach(int item in x) {
		    //     Console.WriteLine(item.ToString());
		    // }
		}
		
		// public int[] SortNumbers(int[] array) {
		//     int[] numbers = array.Where(x => x != -5).OrderBy(x => x).ToArray();
		//     int ctr = 0;
		    
		//     return array.Select(x => x >= 0 ? numbers[ctr++] : -5).ToArray();
		// }
    }
}
