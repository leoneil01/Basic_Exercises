using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.IO;
using System.Linq;
using System.Runtime.Remoting.Metadata.W3cXsd2001;
using System.Text;
using System.Threading.Tasks;

namespace Basic_Exercises
{
    class Program
    {
    	public static void Main(string[] args) {
			// Program program = new Program();
			
			// object[] mixedArray = new object[5];
			// mixedArray[0] = 25;
			// mixedArray[1] = "Ann";
			// mixedArray[2] = false;
			// mixedArray[3] = DateTime.Now;
			// mixedArray[4] = 112.22;
			
			// Console.WriteLine("---Printing array elements and their types---");
			// for(int i = 0; i < mixedArray.Length; i++) {
			//     Console.WriteLine("Value-> {0} :: Type-> {1}", mixedArray[i], mixedArray[i].GetType());
			// }
			
			// string[] newNums = program.Test(mixedArray);
			// Console.WriteLine("\n---Printing array elements and their types---");
			// for(int i = 0; i < newNums.Length; i++) {
			//     Console.WriteLine("Value-> {0} :: Type-> {1}", newNums[i], newNums[i].GetType());
			// }
		}
		
		// public string[] Test(object[] nums) {
		//     return Array.ConvertAll(nums, x => x.ToString());
		// }
    }
}
